name: life_manager
description: "LifeOS — an all-in-one daily life manager: finance, habits, tasks, notes, documents, calendar, goals, and AI-driven insights."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  riverpod_annotation: ^4.0.3
  drift: ^2.34.2
  path: ^1.9.1
  path_provider: ^2.1.6
  fl_chart: ^1.2.0
  flutter_animate: ^4.5.2
  lottie: ^3.3.3
  go_router: ^17.3.0
  file_picker: ^11.0.2
  image_picker: ^1.2.3
  flutter_image_compress: ^2.5.1
  pdfx: ^2.9.2
  photo_view: ^0.15.0
  table_calendar: ^3.2.0
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1
  flutter_timezone: ^4.1.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  uuid: ^4.6.0
  intl: ^0.20.3
  collection: ^1.19.1
  drift_flutter: ^0.3.1
  archive: ^4.0.9
  # Kit §3 mandates the Lucide icon family. It names `lucide_icons`, but that
  # package is pinned to Dart <3.0.0 and last shipped in 2023; this is the
  # maintained port of the same glyph set.
  lucide_icons_flutter: ^3.1.19
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.1
  mocktail: ^1.0.5
  flutter_secure_storage_platform_interface: ^1.1.2
  freezed: ^3.2.5
  json_serializable: ^6.14.0
  path_provider_platform_interface: ^2.1.3
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Manrope (UI/body, weights 400-800) and Newsreader (headings, hero numbers,
  # section titles) are the two families the design comp specifies. Both are
  # shipped as variable fonts — one asset each, with Flutter mapping the
  # requested fontWeight onto the `wght` axis — so all weights the comp uses
  # are available without bundling a file per weight. Kept as local assets
  # rather than `google_fonts` so the app stays fully offline (see CLAUDE.md).
  # Licences: assets/fonts/OFL-manrope.txt, assets/fonts/OFL-newsreader.txt.
  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-Variable.ttf
    - family: Newsreader
      fonts:
        - asset: assets/fonts/Newsreader-Variable.ttf
        - asset: assets/fonts/Newsreader-Italic-Variable.ttf
          style: italic
    # Kit §2: JetBrains Mono is for IDs, file names and code only — never body
    # text. Money uses Manrope with tabular figures, not a monospace face.
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Variable.ttf

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  assets:
    - assets/icon/

# Brand mark from ui-kit/brand. Per BRAND_AND_UI_KIT.md §1 the Android adaptive
# foreground is the ring only, inset to 66% — handing it the full-bleed square
# lets the OS mask and parallax clip the dot off the ring.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icon/icon.png
  min_sdk_android: 21
  adaptive_icon_background: assets/icon/icon_background.png
  adaptive_icon_foreground: assets/icon/icon_foreground.png
  remove_alpha_ios: true
  web:
    generate: true
    image_path: assets/icon/icon.png
    background_color: "#0A6E4D"
    theme_color: "#0E9F6E"
  windows:
    generate: true
    image_path: assets/icon/icon.png

# Splash plates use the brand `bg` token per theme, with the mark in the kit's
# Paper variant on light and Ink on dark — the ring has to invert or it vanishes
# into its own ground.
flutter_native_splash:
  color: "#F5F1E9"
  image: assets/icon/splash_logo.png
  color_dark: "#161512"
  image_dark: assets/icon/splash_logo_dark.png
  android_12:
    image: assets/icon/splash_logo.png
    icon_background_color: "#F5F1E9"
    image_dark: assets/icon/splash_logo_dark.png
    icon_background_color_dark: "#161512"
  web: false
